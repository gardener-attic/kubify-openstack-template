kubify-openstack-template: # todo: edit me after copying
  template: 'default'
  base_definition: ~
  jobs:
    release:
      traits:
        component_descriptor: ~
        version:
          preprocess: 'finalize'
        release:
          nextversion: 'bump_minor'
